fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# settings
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

# DB (async SQLAlchemy + SQLite)
SQLAlchemy>=2.0,<3.0
aiosqlite>=0.19,<1.0

# Redis (optional; can be disabled by REDIS_DISABLED=true)
redis>=5.0,<6.0

# Windows ZoneInfo fallback
tzdata>=2024.1
